describe('Google Login Acceptance Test Suite', () => {
    test('When user clicks login button it should get token, display success, and redirect', () => {
        cy.visit('http://localhost:3000');
    });
});
